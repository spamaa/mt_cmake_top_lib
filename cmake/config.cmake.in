@PACKAGE_INIT@

include(${CMAKE_CURRENT_LIST_DIR}/cmake-top-lib-targets.cmake)
check_required_components(CMakeTopLib)
if (MSVC)
    message (STATUS "!!!!@@ checking CMakeTopLib-asan @@!!!!")
    check_required_components(CMakeTopLib-asan)
endif()